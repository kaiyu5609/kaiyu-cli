{
    "name": "{{metadata.projectName}}",
    "version": "{{metadata.projectVersion}}",
    "description": "{{metadata.projectDescription}}",
    "author": "{{metadata.projectAuthor}}",
    "private": true,
    "scripts": {
        "start": "node index.js"
    },
    "dependencies": {
        "vue": "{{metadata.supportUiVersion}}"
    }
}